# 本地复制为 .env：cp .env.example .env
# Expo 仅将 EXPO_PUBLIC_* 注入客户端，勿把敏感密钥写在可公开变量里。

# 后端 API 根 URL（需含 /api/v1）。不设时默认 https://mmirror.qiexuxing.top/api/v1
# 仅本地连本机后端时在 .env 里写（勿提交）：
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
# Android 模拟器访问本机后端可用 http://10.0.2.2:3001/api/v1
# EAS 构建：eas.json 已注入线上地址；也可用 eas secret 覆盖
