## 1. 工程与基础依赖初始化

- [x] 1.1 初始化/校准 Expo + React Native 工程结构，创建 `app/` 与 `src/` 分层目录（`api`、`stores`、`features`、`components`、`hooks`、`utils`、`types`）。
- [x] 1.2 集成并配置核心依赖：`expo-router`、`zustand`、`axios`、`expo-secure-store`、`react-hook-form`、`zod`、`@tanstack/react-query`、Sentry。
- [x] 1.3 建立基础路由壳与导航分组（未登录区/已登录区），并提供全局错误边界与加载状态容器。

## 2. API 客户端与鉴权链路

- [x] 2.1 在 `src/api/` 实现统一 HTTP 客户端、请求/响应拦截器与标准化错误映射。
- [x] 2.2 实现访问令牌注入规则（受保护接口带 token，公共接口不带），并提供 feature 可复用的 endpoint 封装。
- [x] 2.3 实现 `refreshToken` 安全存储（SecureStore）与启动时会话恢复流程。
- [x] 2.4 实现 401 自动刷新、单飞锁与请求重放队列；刷新失败时统一清理登录态并跳转登录页。
- [x] 2.5 编写 API 契约对齐清单（对照 Web 与 `mind_mirror_api`），补齐关键接口参数与错误码映射测试。

## 3. 认证功能迁移（mobile-auth-session）

- [x] 3.1 完成登录页（表单校验、提交态、错误提示）并接入登录 API。
- [x] 3.2 完成注册页（字段校验、业务错误提示）并接入注册 API。
- [x] 3.3 在个人中心或设置入口实现登出操作，验证本地凭证与缓存清理。
- [x] 3.4 为认证流程补充单测（成功、失败、401 刷新成功/失败场景）。

## 4. 测评主流程迁移（mobile-assessment-flows）

- [x] 4.1 实现测试列表页并接入列表 API（加载、空态、错误重试）。
- [x] 4.2 实现 MBTI 介绍页与答题流程状态管理（题目导航、答案暂存、提交前校验）。
- [x] 4.3 实现 MBTI 结果页与历史页，支持从历史进入结果详情。
- [x] 4.4 实现城市匹配介绍页与答题流程状态管理，并接入提交 API。
- [x] 4.5 实现城市匹配结果页与历史页，保证与 Web 的结果字段展示语义一致。

## 5. 个人中心迁移（mobile-profile-center）

- [x] 5.1 实现个人中心首页，展示基础资料与历史摘要数据。
- [x] 5.2 实现昵称修改流程（表单校验、保存反馈、数据刷新）。
- [x] 5.3 实现密码修改流程（当前密码/新密码校验、失败提示与成功反馈）。
- [x] 5.4 补充个人中心相关单测与接口异常处理测试。

## 6. 稳定性、验收与发布准备

- [x] 6.1 接入 Sentry 基础监控（全局异常、关键请求失败打点入口）。
- [ ] 6.2 执行 iOS/Android 双端冒烟测试：认证、测试列表、MBTI、城市匹配、个人中心全链路。
- [x] 6.3 修复阻塞级问题并形成 MVP 验收清单，确认本期非目标未误入范围。
- [x] 6.4 预留后续 Detox E2E 与性能优化任务占位，作为下一期 backlog。
