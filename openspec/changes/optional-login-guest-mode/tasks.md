## 1. 路由与根入口

- [x] 1.1 修改 `app/index.tsx`：在 `unauthenticated` 时重定向至 `/(main)` 而非 `/(auth)/login`（`loading` 行为保持不变）
- [x] 1.2 修改 `app/(main)/_layout.tsx`：移除未认证时对登录页的强制 `Redirect`；仅在 `loading` 时展示 `AppLoadingView`，`unauthenticated` 与 `authenticated` 均渲染 `Tabs`

## 2. 「我的」与访客体验

- [x] 2.1 在「我的」/个人中心界面区分访客与已登录：访客展示说明与可见的登录、注册入口；已登录保持现有资料与退出能力
- [x] 2.2 核对从访客触发的需身份操作（若有）：失败时提示登录或引导至认证流程，避免静默失败

## 3. HTTP 与会话边界（按需）

- [x] 3.1 审查 `http-client` 与 401 处理：确保访客正常使用不会因全局 `clearSession` 出现意外循环或错误提示（若存在问题则按设计单点调整）

## 4. 验证

- [ ] 4.1 手动验证：冷启动无 token → 进入主区双 Tab → 测评列表可用路径与变更前一致（在公开接口前提下）
- [ ] 4.2 手动验证：访客进入「我的」可找到登录入口；登录成功后 Tab 与 profile 行为与变更前一致；登出后仍可留在主区或符合产品预期
